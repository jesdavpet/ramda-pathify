const { expect } = require(`chai`)
const pathify = require(`../src/index`)

describe(`Tag pathify\`...\``, () => {
  it(`should fail until test casess are written`, () => {
    expect(true).to.be(false)
  })
})
